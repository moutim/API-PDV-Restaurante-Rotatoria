const errorHandler = require('./errorHandler');
const verifyCreateCliente = require('./validateCliente');
const verifyCreateProduto = require('./validateProduto');

module.exports = {
  errorHandler,
  verifyCreateCliente,
  verifyCreateProduto,
};
